# 🧠 SimpleStore

`SimpleStore` — это потокобезопасное (thread-safe) хранилище пар **ключ → значение**, 
где ключ — строка, а значение — массив байтов.  
Класс поддерживает базовые операции `Set`, `Get`, `Delete`, 
а также предоставляет статистику использования.

## ✨ Возможности

- Потокобезопасные операции чтения и записи с помощью `ReaderWriterLockSlim`
- Поддержка подсчёта количества вызовов:
    - `Set` — количество добавлений/обновлений значений
    - `Get` — количество чтений
    - `Delete` — количество успешных удалений
- Поддержка корректной очистки ресурсов (`IDisposable`)
- Простая, быстрая и безопасная реализация на основе `Dictionary<string, byte[]>`

---

## 🚀 Пример использования

```csharp
using System;
using System.Text;

class Program
{
    static void Main()
    {
        using var store = new SimpleStore();

        // Добавление значений
        store.Set("key1", Encoding.UTF8.GetBytes("Hello"));
        store.Set("key2", Encoding.UTF8.GetBytes("World"));

        // Получение значения
        var value = store.Get("key1");
        Console.WriteLine(Encoding.UTF8.GetString(value!)); // Вывод: Hello

        // Удаление значения
        store.Delete("key2");

        // Получение статистики
        var stats = store.GetStatistics();
        Console.WriteLine($"Set: {stats.setCount}, Get: {stats.getCount}, Delete: {stats.deleteCount}");
    }
}